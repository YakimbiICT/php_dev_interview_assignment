<div data-action="setIsFavorite" >
    <a href="#" class="is-favorite-true" >
        <i class="icon-star"></i>
        Remove from Favorites
    </a><br/>
    {% if image.description %}
        {% include '_description-true.html.twig' with {description: image.description} %}
    {% else %}
        {% include '_description-false.html.twig' with {description: image.description} %}
    {% endif %}
        
</div>